import { OrderProductPipe } from './order-product.pipe';

describe('OrderProductPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderProductPipe();
    expect(pipe).toBeTruthy();
  });
});
