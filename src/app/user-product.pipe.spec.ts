import { UserProductPipe } from './user-product.pipe';

describe('UserProductPipe', () => {
  it('create an instance', () => {
    const pipe = new UserProductPipe();
    expect(pipe).toBeTruthy();
  });
});
